@model IEnumerable<VFoods.tbl_payments>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    

    <a href="@Url.Action("Create", "Payments")" class="btn btn-primary">
        <span>
            <i class="fas fa-plus-circle"></i>

        </span>
        <span>
            <strong>Add New Payment </strong>
        </span>
    </a>
</p>
<table class="table table-striped">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.date)
        </th>
    
        <th>
            @Html.DisplayNameFor(model => model.Amount)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Comment)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tbl_CustomerDetails.Account_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.date)
        </td>
      
        <td>
            @Html.DisplayFor(modelItem => item.Amount)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Comment)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tbl_CustomerDetails.Account_name)
        </td>
        @*<td>
           

            <a href="@Url.Action("Edit", "Payments", new { id = item.id })" class="btn btn-primary">
                <span>
                    <i class="fas fa-edit"></i>
                </span>
            </a> |
            <a href="@Url.Action("Details", "Payments", new { id = item.id })" class="btn btn btn-success">
                <span>
                    <i class="fas fa-list"></i>
                </span>
            </a> |
            <a href="@Url.Action("Delete", "Payments", new { id = item.id })" class="btn btn-danger">
                <span>
                    <i class="fas fa-trash-alt"></i>
                </span>
            </a>
        </td>*@
    </tr>
}

</table>
